type StripeCharge {
    "Id del Pago"
    id: ID
    "Cantidad que hemos pagado en mx"
    amount: Float!
    "Estado del periodo"
    status: String!
    "Email donde se va a enviar la factura"
    receiptEmail: String!
    "Factura con la URL asiciada del pedido"
    receiptUrl: String!
    "Pagado o no"
    paid: Boolean
    created: String!
    "operacion que se realiza- pago o description"
    typeOrder: String!
    description: String!
    card: ID!
    customer: ID!
}