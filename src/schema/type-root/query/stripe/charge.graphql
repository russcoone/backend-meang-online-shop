# type Query{
#     chargesByCustomer(
#         customer: ID!,
#         limit: Int = 5,
#         startingAfter: ID = "",
#         endingBefore: ID = ""): ResultStripeCharge
    
# }
type Query {
     chargesByCustomer(
        customer: ID!,
        limit: Int = 5,
        startingAfter: ID = "",
        endingBefore: ID = ""
    ): ResultStripeCharge
}